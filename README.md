# PRODIGY_ST_03
Automated login testing was performed on a demo web application using Selenium. The test suite includes positive cases (valid username and password → successful login) and negative cases (invalid credentials, empty fields → error messages). This ensures the login functionality works correctly and handles incorrect inputs gracefully.
